---
name: transcript-to-insights
description: >
  Analyze meeting/interview transcripts to extract structured outputs. Uses an alignment-first workflow to probe user intent and clarify background context before generating insights.
---

# 调研会议转录文稿洞察提炼

核心目标：彻底抛弃僵化的“固定模板填空”，转向“对齐优先”的柔性工作流。
在生成最终报告前，必须通过对话与用户充分对齐场景意图、补充缺失背景，而后根据真实需求动态组织提取框架。

## 产出物要求

无论输入什么文件（如 `xxx_cleaned.md`），最终在同级目录生成报告，默认命名为 `xxx_insights.md`（或根据最终确定的场景自拟合理文件名）。

## 第一步：初探与背景探询 (Context Probing)

**【绝对强制动作】：收到文稿后，禁止立刻开始生成最终报告！**

1. **粗读文稿**：快速通读文稿，识别出核心议题、涉及的场景以及文本中存在歧义、缺失背景的地方。
2. **主动提问（1-3个关键问题）**：向用户发起对话，探寻提炼意图并补足背景。提问方向参考：
   - **探寻核心意图**：“这份总结的主要受众是谁？你想用它来推进什么决策？（比如是高层汇报、提炼产品需求、还是内部争论复盘？）”
   - **补足业务背景**：“文中多处提到了 `[某个模糊术语/代号]`，为了准确判断，这具体是指你们的什么系统或角色？”
   - **聚焦关注点**：“大家既聊了技术痛点，又聊了跨部门流程，你希望我这次的提炼重点放大哪一部分？”

## 第二步：多轮互动对齐 (Multi-round Alignment)

- 倾听用户的回答并进行评估。如果意图依然含糊，大胆继续追问。
- 如果用户意图已经清晰，简要向用户复述自己即将执行的提取策略（例如：“收到，我将侧重于发掘操作工的实际痛点，暂略管理层的宏观愿景，这就为您提取洞察。”），获得首肯后开始执行。
- **兜底方案**：如果用户明确表示“不想回答，随便总结一下”或要求跳过对齐，则放弃追问，采用通用摘要策略（提取核心结论、主要分歧和下一步计划）直接生成结构化报告。

## 第三步：高度定制的动态提取 (Dynamic Extraction)

在获得充足的背景信息和明确意图后，开始生成最终报告。

- **动态排版**：不预设固定的标题结构（不再强制分块为业务场景、用户画像等）。根据对话得出的真实目标，自由组织最具可读性和逻辑性的结构，直击用户核心诉求。
- **质量与底线约束**：
  - 所有的深度结论和判断，都必须附带具体的原文依据引用（如标注 `@Line X` 或 `@发言人`）。
  - 如果原稿材料不足以支撑用户关注的某个方向，必须在报告中坦诚说明“当前文稿材料不足以支撑该结论”，严禁无中生有、脑补发散。

## 专有名词与词表沉淀 (CLAUDE.md)

若在提炼和与用户沟通的过程中，明确了跨文档的专有名词或特定业务术语的真实含义，需将其记录到同级目录的 `CLAUDE.md` 中。
- **若 `CLAUDE.md` 已存在，直接追加写入到文件末尾。**
- 若不存在，则创建并写入。

## 质量自查清单

执行动作与生成报告前自查：
- [ ] 拿到文稿后，是否克制住了直接总结的冲动，先发起了有价值的对齐提问？
- [ ] 最终报告的结构是否是为用户真实意图“量身定制”的，而不是生搬硬套模板？
- [ ] 所有的洞察和非共识结论是否都附带了确切的原文依据？
- [ ] 遇到文稿支撑不了的诉求，是否果断说明了“证据不足”？
